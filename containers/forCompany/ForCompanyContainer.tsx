import Layout from "@/components/_shared/core/layout";
import ForCompanyComponent from "@/components/forCompany/ForCompanyComponent";

const ForCompanyContainer = () => {
  return (
    <Layout>
      <ForCompanyComponent />
    </Layout>
  );
};

export default ForCompanyContainer;
